#:import BlankNavItem presenter.navbar
#:import DeliveryList presenter.delivery_list
#:import BlankNavItem presenter.navbar
#:import DeliveryRequestDetail presenter.delivery_request_detail


<NavBarWithFAB>:
    fab_diameter: 64

    FloatLayout:
        MDBottomNavigation:
            id: navbar

            MDBottomNavigationItem:
                name: 'map'
                text: 'Map'
                icon: 'map-marker'

                DeliveryList:

            MDBottomNavigationItem:
                name: 'my-packages'
                text: 'My Packages'
                icon: 'package'

                on_tab_press: root._load_posted_requests(self)

            BlankNavItem:
                icon: 'dots-horizontal'

            MDBottomNavigationItem:
                name: 'my-deliveries'
                text: 'My Deliveries'
                icon: 'bike'

                on_tab_press: root._load_my_deliveries(self)

            MDBottomNavigationItem:
                name: 'my-account'
                text: 'My Account'
                icon: 'account-circle'

                on_tab_press: root._load_profile_view(self)

        MDFloatingActionButton:
            id: fab
            x: navbar.width // 2 - dp(root.fab_diameter) // 2
            y: navbar.children[0].height // 2
            size: (dp(root.fab_diameter), dp(root.fab_diameter))
            elevation_normal: 8
            md_bg_color: (217/255, 217/255, 217/255, 1)
            icon: 'package-variant'
